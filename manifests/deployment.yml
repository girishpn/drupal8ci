apiVersion : apps/v1
kind: Deployment
metadata:
  name: "aks1-9dbf"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: "aks1-9dbf"
  template:
    metadata:
      labels:
        app: "aks1-9dbf"
    spec:
      containers:
        - name: "aks1-9dbf"
          image: "gpreg1.azurecr.io/aks1"
          ports:
          - containerPort: 6480